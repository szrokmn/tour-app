import React from 'react'
import {data} from '../../assests/data'

const Main = () => {
    // console.log(data);    
  return (
    <div className='card-container'>
      {data.map((item) => co)}
    </div>
  )
}
export default Main
